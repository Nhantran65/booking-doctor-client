export enum HTTP_CODE {
    SUCCESS = 200,
    NOT_FOUND = 404,
    BAD_REQUEST = 400,
    WRONG_PASSWORD = 701,
    EMAIL_EXIST = 724,
    NOT_PERMISSION = 209,
    SCORE_FAILED = -1,
    MAX_DEVICE = 405,
}
export enum FETCH_STATUS {
    FETCHING = "fetching",
}
